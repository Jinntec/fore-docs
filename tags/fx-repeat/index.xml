<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Fx-Repeat on Fore Documentation</title><link>https://jinntec.github.io/fore-docs/tags/fx-repeat/</link><description>Recent content in Fx-Repeat on Fore Documentation</description><generator>Hugo</generator><language>en-us</language><lastBuildDate>Tue, 21 Jul 2026 12:00:00 +0200</lastBuildDate><atom:link href="https://jinntec.github.io/fore-docs/tags/fx-repeat/index.xml" rel="self" type="application/rss+xml"/><item><title>&lt;fx-repeat></title><link>https://jinntec.github.io/fore-docs/elements/ui/repeat/</link><pubDate>Tue, 14 Dec 2021 17:41:11 +0100</pubDate><guid>https://jinntec.github.io/fore-docs/elements/ui/repeat/</guid><description>&lt;h2 id="description">Description&lt;/h2>
&lt;p>Repeats template for each node of the referenced nodeset.&lt;/p>
&lt;p>For each data node in the referenced nodeset one &lt;code>&amp;lt;fx-repeatitem&amp;gt;&lt;/code> element
will be created that will contain the evaluated template as content.&lt;/p>
&lt;h2 id="attributes">Attributes&lt;/h2>
&lt;table>
 &lt;thead>
 &lt;tr>
 &lt;th>Name&lt;/th>
 &lt;th>Description&lt;/th>
 &lt;/tr>
 &lt;/thead>
 &lt;tbody>
 &lt;tr>
 &lt;td>id&lt;/td>
 &lt;td>identifier for repeat&lt;/td>
 &lt;/tr>
 &lt;tr>
 &lt;td>&lt;em>&lt;strong>ref&lt;/strong>&lt;/em>&lt;/td>
 &lt;td>XPath reference pointing to the bound node&lt;/td>
 &lt;/tr>
 &lt;tr>
 &lt;td>size&lt;/td>
 &lt;td>Caps how many &lt;code>fx-repeatitem&lt;/code> elements are materialized from the (always-complete) bound nodeset. Without &lt;code>virtual&lt;/code>, this is a growing prefix cap: a trailing sentinel reveals the next &lt;code>size&lt;/code> items via &lt;code>IntersectionObserver&lt;/code> as the user scrolls, and once rendered, rows are never un-rendered. With &lt;code>virtual&lt;/code>, &lt;code>size&lt;/code> instead becomes the size of a sliding window. Omit for full, uncapped rendering (the default).&lt;/td>
 &lt;/tr>
 &lt;tr>
 &lt;td>virtual&lt;/td>
 &lt;td>Opt-in true windowed virtualization, only meaningful together with a finite &lt;code>size&lt;/code>. Instead of an ever-growing prefix, the DOM stays bounded at roughly &lt;code>size&lt;/code> rows regardless of nodeset size: scrolling down appends rows below and evicts rows from the top; scrolling up prepends rows above and evicts from the bottom. &lt;code>virtual&lt;/code> without &lt;code>size&lt;/code> is ignored (behaves as fully uncapped).&lt;/td>
 &lt;/tr>
 &lt;/tbody>
&lt;/table>
&lt;h2 id="progressive-rendering-and-virtualization">Progressive rendering and virtualization&lt;/h2>
&lt;p>&lt;code>size&lt;/code> only limits &lt;em>DOM materialization&lt;/em>. The logical nodeset - and therefore &lt;code>index()&lt;/code>, &lt;code>iterate&lt;/code> actions, and explicit &lt;code>fx-bind&lt;/code> model semantics (&lt;code>calculate&lt;/code>, &lt;code>constraint&lt;/code>, &lt;code>required&lt;/code>, &lt;code>readonly&lt;/code>, &lt;code>relevant&lt;/code>) - always covers every item, regardless of how many rows are currently rendered. Programmatic or keyboard navigation to an index beyond the currently rendered window (e.g. via &lt;code>setIndex&lt;/code>, an &lt;code>index('id')&lt;/code>-driven dependent, or an insert/append past the window) materializes rows on demand: without &lt;code>virtual&lt;/code>, it front-fills up through the target row; with &lt;code>virtual&lt;/code>, it performs a hard jump - the current window is discarded and a fresh one is rendered starting at the target index, resetting scroll position to the top of that window.&lt;/p></description></item><item><title>&lt;fx-repeat-ref></title><link>https://jinntec.github.io/fore-docs/elements/ui/repeat-ref/</link><pubDate>Tue, 21 Jul 2026 12:00:00 +0200</pubDate><guid>https://jinntec.github.io/fore-docs/elements/ui/repeat-ref/</guid><description>&lt;h2 id="description">Description&lt;/h2>
&lt;p>&lt;code>fx-repeat-ref&lt;/code> is a placeholder used inside an &lt;a href="https://jinntec.github.io/fore-docs/elements/ui/repeat/">&lt;code>fx-repeat&lt;/code>&lt;/a>
template to recurse into a data node&amp;rsquo;s own matching children - the same idea as
XSLT&amp;rsquo;s &lt;code>apply-templates&lt;/code>. It lets one template render an arbitrary-depth,
self-similar tree (file/directory trees, org charts, threaded comments, nested
categories, outline structures) instead of a hand-written, depth-capped chain
of nested &lt;code>fx-repeat&lt;/code> elements.&lt;/p>
&lt;p>Place &lt;code>&amp;lt;fx-repeat-ref&amp;gt;&lt;/code> anywhere inside the nearest ancestor &lt;code>fx-repeat&lt;/code>&amp;rsquo;s
template. On connect, it synthesizes a new &lt;code>fx-repeat&lt;/code> bound to that same
&lt;code>ref&lt;/code> (or an explicit override, see below), wraps a clone of the ancestor&amp;rsquo;s
template around it, and replaces itself with that new repeat. The new repeat
then goes through the same lifecycle as any other: if its own template
contains another &lt;code>&amp;lt;fx-repeat-ref&amp;gt;&lt;/code>, recursion continues one level deeper,
scoped to the current item via the usual nested-repeat context resolution.
Recursion terminates naturally wherever a node has no more matching children -
no depth check or &lt;code>max-depth&lt;/code> attribute is needed for correctness.&lt;/p></description></item></channel></rss>